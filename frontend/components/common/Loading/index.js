import React from 'react';

import {
  LoadingWrapper,
  LoadingIcon,
} from './style';

const Loading = () => {
  return (
    <LoadingWrapper>
      <LoadingIcon type="loading" />
    </LoadingWrapper>
  );
};

export default Loading;
